<?xml version="1.0" encoding="utf-8"?>
<!-- [예빈] 아카이브 가치별 빈도 그래프 아이템 -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="#F4F4F9"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp">

        <!-- 1. 좌측 원형 흰색 배경 래퍼 -->
        <FrameLayout
            android:id="@+id/layout_symbol_bg"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_circle_white"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/iv_value_symbol"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                tools:src="@drawable/ic_symbol_relation" />
        </FrameLayout>

        <!-- 2. 가치 이름 (예: 관계) -->
        <TextView
            android:id="@+id/tv_value_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:textColor="@color/text_black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@+id/layout_symbol_bg"
            app:layout_constraintTop_toTopOf="@+id/layout_symbol_bg"
            tools:text="관계" />

        <!-- 3. 우측 화살표 -->
        <ImageView
            android:id="@+id/iv_arrow"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_arrow_right"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="#888888" />

        <!-- 4. 기록 개수 (예: 기록 12개) -->
        <TextView
            android:id="@+id/tv_value_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:textColor="#999999"
            android:textSize="13sp"
            app:layout_constraintBottom_toBottomOf="@+id/tv_value_name"
            app:layout_constraintEnd_toStartOf="@+id/iv_arrow"
            app:layout_constraintTop_toTopOf="@+id/tv_value_name"
            tools:text="기록 12개" />

        <!-- 5. 가치별 비율 게이지 바 (ProgressBar) -->
        <ProgressBar
            android:id="@+id/progress_value"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="8dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:max="100"
            android:progressDrawable="@drawable/bg_archive_progress"
            app:layout_constraintEnd_toStartOf="@+id/iv_arrow"
            app:layout_constraintStart_toStartOf="@+id/tv_value_name"
            app:layout_constraintTop_toBottomOf="@+id/tv_value_name"
            tools:progress="80" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>